import { AlertTriangle, ArrowLeft, BarChart3, PieChart as PieChartIcon, WalletCards } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import { Bar, BarChart, CartesianGrid, Cell, Legend, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { useTripData } from '../../hooks/useTripSelectors'
import { formatCategoryLabel, formatCurrency, formatDateRange } from '../../lib/formatters'
import { Badge } from '../../components/ui/Badge'
import { Button } from '../../components/ui/Button'
import { Card, MetricCard, SectionHeading } from '../../components/ui/Card'
import { EmptyState } from '../../components/ui/Feedback'
import { ProgressBar } from '../../components/ui/ProgressBar'
import { ProportionBar } from '../../components/ui/ProportionBar'
import type { ExpenseCategory } from '../../types/domain'

const chartColors: Record<ExpenseCategory, string> = {
  transportation: '#2A3439',
  accommodation: '#A45A52',
  activities: '#85B09A',
  food: '#D8A88F',
  other: '#9EA6A5',
}

export function BudgetPage() {
  const { tripId } = useParams()
  const data = useTripData(tripId)
  if (!data) return <EmptyState title="Budget not found" description="We could not find a trip for this budget view." action={<Button asChild><Link to="/trips">Back to my trips</Link></Button>} />
  const categoryData = (Object.entries(data.budget.categories) as [ExpenseCategory, number][]).map(([category, amount]) => ({ name: formatCategoryLabel(category), amount, category }))
  const overBudget = data.budget.remaining !== null && data.budget.remaining < 0
  const percentUsed = data.budget.budgetLimit ? (data.budget.total / data.budget.budgetLimit) * 100 : 0

  return (
    <div className="space-y-8"><div className="flex flex-wrap items-center justify-between gap-4"><Link to={`/trips/${data.trip.id}/itinerary`} className="inline-flex items-center gap-2 text-sm font-semibold text-ink/55 hover:text-ink"><ArrowLeft size={16} />Back to itinerary</Link><Badge tone={overBudget ? 'clay' : 'sage'}>{overBudget ? 'Over budget' : 'Within budget'}</Badge></div><SectionHeading eyebrow="Spend with intention" title="Budget & cost breakdown" description={`${data.trip.name} · ${formatDateRange(data.trip.startDate, data.trip.endDate)}`} /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card className="bg-ink text-parchment"><WalletCards size={19} className="text-sage" /><p className="eyebrow mt-5 text-parchment/60">Total estimated cost</p><p className="mt-2 font-display text-3xl">{formatCurrency(data.budget.total)}</p><p className="mt-1 text-xs text-parchment/55">Across planned items</p></Card><Card><MetricCard label="Cost per day" value={formatCurrency(data.budget.costPerDay)} detail="Average for this route" /></Card><Card><MetricCard label="Budget limit" value={data.budget.budgetLimit ? formatCurrency(data.budget.budgetLimit) : 'Not set'} detail="Your working comfort zone" /></Card><Card><MetricCard label="Remaining" value={data.budget.remaining === null ? '—' : formatCurrency(data.budget.remaining)} detail={overBudget ? 'A small rethink may help' : 'Room for one more good idea'} accent={overBudget ? 'clay' : 'sage'} /></Card></div>{data.budget.budgetLimit ? <Card className={overBudget ? 'border-clay/40 bg-clay/5' : ''}><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="eyebrow">Budget pulse</p><h2 className="mt-2 font-display text-2xl font-medium text-ink">{overBudget ? 'The route is asking for a little edit.' : 'You are travelling within your plan.'}</h2></div>{overBudget ? <AlertTriangle size={22} className="text-clay" aria-hidden="true" /> : null}</div><ProgressBar value={data.budget.total} max={data.budget.budgetLimit} tone={overBudget ? 'clay' : 'sage'} className="mt-5 h-3" /><div className="mt-3 flex justify-between text-xs text-ink/55"><span>{Math.round(percentUsed)}% of budget used</span><span>{formatCurrency(Math.abs(data.budget.remaining || 0))} {overBudget ? 'over' : 'left'}</span></div></Card> : null}<div className="grid gap-6 xl:grid-cols-[minmax(0,1.1fr)_minmax(20rem,0.9fr)]"><Card><div className="flex items-start justify-between gap-4"><div><p className="eyebrow">Where it goes</p><h2 className="mt-2 font-display text-2xl font-medium text-ink">Budget proportions</h2></div><PieChartIcon size={20} className="text-clay" aria-hidden="true" /></div><div className="mt-6"><ProportionBar categories={data.budget.categories} total={data.budget.total} /></div><div className="mt-7 space-y-4">{categoryData.map((item) => <div key={item.category} className="grid grid-cols-[8rem_minmax(0,1fr)_auto] items-center gap-3 text-sm"><span className="truncate text-ink/60">{item.name}</span><ProgressBar value={item.amount} max={Math.max(data.budget.total, 1)} tone={item.category === 'activities' ? 'sage' : item.category === 'transportation' ? 'ink' : 'clay'} /><span className="font-semibold text-ink">{formatCurrency(item.amount)}</span></div>)}</div></Card><Card><div className="flex items-start justify-between gap-4"><div><p className="eyebrow">Visual breakdown</p><h2 className="mt-2 font-display text-2xl font-medium text-ink">Category mix</h2></div><BarChart3 size={20} className="text-sage" aria-hidden="true" /></div><div className="mt-5 h-64 w-full"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={categoryData.filter((item) => item.amount > 0)} dataKey="amount" nameKey="name" cx="50%" cy="46%" innerRadius={58} outerRadius={88} paddingAngle={3}>{categoryData.filter((item) => item.amount > 0).map((item) => <Cell key={item.category} fill={chartColors[item.category]} />)}</Pie><Tooltip formatter={(value) => formatCurrency(Number(value))} contentStyle={{ borderRadius: '12px', borderColor: '#D0D0CC', backgroundColor: '#F4F0EC', fontSize: '12px' }} /><Legend iconType="circle" wrapperStyle={{ fontSize: '11px' }} /></PieChart></ResponsiveContainer></div></Card></div><Card><div className="flex items-start justify-between gap-4"><div><p className="eyebrow">Compare categories</p><h2 className="mt-2 font-display text-2xl font-medium text-ink">Estimated spend</h2></div><BarChart3 size={20} className="text-ink/55" aria-hidden="true" /></div><div className="mt-5 h-72 w-full"><ResponsiveContainer width="100%" height="100%"><BarChart data={categoryData} margin={{ top: 8, right: 8, left: 0, bottom: 4 }}><CartesianGrid vertical={false} stroke="#D0D0CC" strokeDasharray="3 3" /><XAxis dataKey="name" tick={{ fontSize: 11, fill: '#2A3439' }} axisLine={false} tickLine={false} /><YAxis tick={{ fontSize: 11, fill: '#2A3439' }} axisLine={false} tickLine={false} tickFormatter={(value) => `₹${Number(value) / 1000}k`} /><Tooltip formatter={(value) => formatCurrency(Number(value))} contentStyle={{ borderRadius: '12px', borderColor: '#D0D0CC', backgroundColor: '#F4F0EC', fontSize: '12px' }} /><Bar dataKey="amount" radius={[5, 5, 0, 0]}>{categoryData.map((item) => <Cell key={item.category} fill={chartColors[item.category]} />)}</Bar></BarChart></ResponsiveContainer></div></Card></div>
  )
}
